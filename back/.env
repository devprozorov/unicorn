APP_ENV=dev
HTTP_ADDR=:8080

MONGO_URI=
MONGO_DB=unicorn

# JWT HS256 secret (48+ chars; ideally 64+ random bytes)
JWT_HS256_SECRET=CHANGE_ME_TO_LONG_RANDOM_64+_BYTESCHANGE_ME_TO_LONG_RANDOM_64+_BYTES

# AES-256-GCM key for encrypting TOTP secret (base64 of 32 bytes)
# generate: head -c 32 /dev/urandom | base64
TOTP_ENC_KEY_B64=

# Cookies
COOKIE_SECURE=false
COOKIE_DOMAIN=unicornstar.online
COOKIE_SAMESITE=Strict

# CORS (comma-separated)
CORS_ORIGINS=http://unicornstar.online
# http://localhost:3000 for local dev

# Admin Bootstrap (создается при запуске если не существует)
ADMIN_BOOTSTRAP_LOGIN=SomeUser
ADMIN_BOOTSTRAP_PASSWORD=

# Robokassa Settings
ROBOKASSA_MERCHANT_LOGIN=
ROBOKASSA_PASSWORD1=
ROBOKASSA_PASSWORD2=
ROBOKASSA_TEST_MODE=false
SUBSCRIPTION_PRICE=250.00
SUBSCRIPTION_DURATION_DAYS=30
